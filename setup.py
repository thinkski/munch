from setuptools import setup

setup(name='munch',
      version='0.1',
      description='Command-line interface to https://munchery.com',
      url='https://github.com/thinkski/munch',
      author='Chris Hiszpanski',
      license='MIT',
      scripts=['scripts/munch'],
      packages=['munchery'])
